name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout the repository
    - name: Checkout code
      uses: actions/checkout@v3

    # Step 2: Set up Node.js
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    # Step 3: Install dependencies
    - name: Install dependencies
      run: npm install
      working-directory: ./client  # Add this if package.json is inside the 'client' folder

    # Step 4: Build the React app
    - name: Build React app
      run: npm run build
      working-directory: ./client  # Add this if package.json is inside the 'client' folder

    # Step 5: Build Docker image
    - name: Build Docker image
      run: docker build -t my-react-app .
      working-directory: ./client  # Add this if package.json is inside the 'client' folder

    # Step 6: Log in to Docker Hub (or your Docker registry)
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: naveenprasaath29@gmail.com
        password: QAZwsc1729/*-+

    # Step 7: Push Docker image
    - name: Push Docker image
      run: |
        docker tag my-react-app naveenprasaath29@gmail.com/my-react-app:v1
        docker push naveenprasaath29@gmail.com/my-react-app:v1
